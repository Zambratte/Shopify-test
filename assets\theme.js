document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-variant-select]').forEach(select=>{select.addEventListener('change',()=>{const option=select.options[select.selectedIndex];const form=select.closest('form');form.querySelector('[name="id"]').value=option.value;const price=document.querySelector('[data-product-price]');if(price&&option.dataset.price)price.textContent=option.dataset.price})})});
